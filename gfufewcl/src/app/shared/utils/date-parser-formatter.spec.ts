import { DateParserFormatter } from './date-parser-formatter';

describe('DateParserFormatter', () => {
  it('should create an instance', () => {
    expect(new DateParserFormatter()).toBeTruthy();
  });
});
